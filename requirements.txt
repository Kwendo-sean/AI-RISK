# Pinned to the versions this project is developed and tested against.
# Application
fastapi==0.135.2
uvicorn[standard]==0.42.0
gunicorn==23.0.0
pydantic==2.12.5
email-validator==2.3.0
python-dotenv==1.2.2
httpx==0.28.1

# Database
aiosqlite==0.22.1          # local dev and offline single-node (Raspberry Pi)
asyncpg==0.30.0            # production PostgreSQL

# Optional AI enrichment (a static fallback is used when this is absent)
anthropic==0.86.0

# Content build + lead export
openpyxl==3.1.5
